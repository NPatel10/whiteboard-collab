# Canvas Choice

Use `konva` with `svelte-konva` for the whiteboard surface.

Why this choice:
- `svelte-konva` gives us an official Svelte integration instead of a custom wrapper.
- `konva` keeps the board model and layer handling practical for later PNG/image export work.
